<%= simple_form_for @event, :class=>"form-horizontal display-block", url: wizard_path do |f| %>
  <% if @event.errors.any? %>
    <div class="error_messages">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= yield f%>
<div class="actions">
  <div class="pull-left">
  <%=link_to "Schritt zurück", previous_wizard_path%></div>
  
  <div class="pull-right">
<%= f.submit "Weiter >>", class: "btn btn-primary"%>
    or <%= link_to "skip this step", next_wizard_path %>
  </div>
</div>

<% end %>

